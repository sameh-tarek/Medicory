server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/medicory
    username: medicory
    password: Medicory.1
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
rsa:
  private-key: classpath:certs/private.pem
  public-key: classpath:certs/public.pem